const $=id=>document.getElementById(id);
const money=n=>Number.isFinite(+n)?new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2}).format(+n):'—';
const num=n=>Number.isFinite(+n)?new Intl.NumberFormat('en-US',{maximumFractionDigits:2}).format(+n):'—';
function calcRisk(){const b=+$('riskBalance').value,p=+$('riskPct').value,e=+$('entry').value,s=+$('sl').value,t=+$('tp').value,v=+$('pointValue').value;const risk=b*p/100,dist=Math.abs(e-s),reward=Math.abs(t-e),rr=dist?reward/dist:NaN;let units=dist&&v?risk/(dist*v):NaN;$('riskResult').innerHTML=[['المبلغ المعرض للخطر',money(risk)],['مسافة SL',num(dist)],['R:R',Number.isFinite(rr)?`1 : ${rr.toFixed(2)}`:'—'],['الحجم النظري',Number.isFinite(units)?num(units):'غير محدد']].map(x=>`<div class="mini-card"><small>${x[0]}</small><strong style="display:block;font:900 21px Montserrat;direction:ltr;margin-top:3px">${x[1]}</strong></div>`).join('')}
document.addEventListener('DOMContentLoaded',()=>{if($('calcRisk'))$('calcRisk').onclick=calcRisk;if($('calcRisk'))calcRisk();document.querySelector('[data-menu]')?.addEventListener('click',()=>document.querySelector('.links')?.classList.toggle('open'));});
